/**
 * @agenthub/dsh-client-ui-market-data —— 金融分析师市场数据看板面板（host 半部）。
 * 纯 UI 插件：空 apply 使其出现在 host cordis.yml / Loader；
 * 浏览器半部经 exports["./client"] 提供，由 package.json 的 dsh.client 声明发现。
 */
function apply() {}
export { apply };
