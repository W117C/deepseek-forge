# v0.3 Phase D 实施记录：收尾（bin 别名 / 状态管理 / 令牌轮换）

> 状态：✅ 完成（18 套 e2e 245/245 全绿，新增 e2e-phase-d 15/15）。

## 变更内容

- **forge bin 别名**：package.json bin 增加 forge → 同一 CLI（保留 agenthub 兼容）；版本号 0.3.0（根与 forge 站点对齐）。
- **包状态管理端点**：POST /v1/packages/:id/status（operatorToken 鉴权）：published | deprecated | yanked；blocked 包不可恢复为 published；非法状态 400、无令牌 401。
- **客户端状态门禁升级**：blocked 与 yanked 直接阻断安装（明确错误文案）；deprecated 放行（市场语义：仍可安装，不推荐）。
- **发布者令牌轮换**：POST /v1/publishers/:id/rotate-token（旧 Bearer 校验 → 签发新令牌、旧失效）；无令牌 401；audit_logs 记录。
- **修复**：bearer 提取从 publish 路由局部提升到请求入口（TDZ——轮换路由先于其声明使用）。

## 验证（e2e-phase-d 15/15）

- bin 别名与版本号声明；轮换→旧令牌 401/新令牌 200/无令牌 401；yank→视图 status 更新→安装阻断→deprecated 可装→恢复 published→无令牌 401→非法状态 400。
