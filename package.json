{
  "name": "agenthub",
  "version": "0.2.0",
  "description": "DeepSeek Harness Agent Bundle Marketplace — CLI installer, registry and marketplace web",
  "type": "module",
  "bin": { "agenthub": "./cli/agenthub.mjs" },
  "files": ["cli", "lib", "bundles", "README.md", "LICENSE"],
  "repository": {
    "type": "git",
    "url": "https://github.com/W117C/deepseek-forge.git"
  },
  "keywords": ["deepseek", "harness", "agent", "bundle", "marketplace", "cli"],
  "engines": { "node": ">=22" },
  "license": "MIT",
  "scripts": {
    "test:e2e": "bash -c 'for t in test/e2e*.mjs; do node \"$t\" || exit 1; done'"
  }
}
